# 직접 해보기

내 지갑을 연결해 **내 전용 에이전트**에게 일을 맡기고, 내가 정한 한도가 실제로 지출을 막는지 확인하는 안내입니다.

**라이브**: <https://a2a-house.8-230-9-237.nip.io/#me>

두 갈래가 있습니다. 웹만 써도 전 과정을 볼 수 있고(5분), MCP까지 붙이면 원래 설계대로 **터미널에서 에이전트에게 일을 시키게** 됩니다(추가 5분).

---

## A. 웹만으로 (5분)

### 1. 지갑 연결

`#me`로 들어가 지갑을 연결합니다.

- **D'CENT · Phantom**: 설치돼 있으면 버튼에 지갑 이름이 뜹니다. 눌러서 서명하면 됩니다.
- **지갑이 없다면**: "임시 체험 지갑으로 시작"을 누르세요. 브라우저 안에서만 키를 만들어 씁니다. 서명은 그대로 브라우저에서 일어나므로 흐름은 동일합니다.

연결하는 순간 **본인 전용 에이전트 지갑**이 발급됩니다. 서버가 계정을 만들어 주는 게 아니라, 연결한 주소가 곧 신원입니다.

### 2. 체험 자산 받기

"체험용 USDC 받기"를 누르면 **내 지갑**에 100 USDC와 소액 SOL이 들어옵니다.

에이전트 지갑이 아니라 내 지갑에 들어온다는 점이 중요합니다. 에이전트는 아직 한 푼도 못 씁니다.

> devnet SOL은 총량 상한(3 SOL) 안에서 지급합니다. 소진되면 USDC만 지급되고, 화면이 <https://faucet.solana.com> 을 안내합니다.

### 3. 에이전트에게 맡기기 (= 위임)

가운데 "맡기기"에 금액을 넣고 **입금**을 누릅니다. 지갑이 서명을 요청합니다.

**이 입금이 곧 위임입니다.** 보낸 만큼만 에이전트가 쓸 수 있고, 그 금액이 정책 한도의 상한이 됩니다. 서버는 서명 없는 트랜잭션을 조립만 하고 여러분의 키를 갖지 않습니다. 에이전트가 쓸 가스(SOL)도 같은 트랜잭션에 함께 실려 갑니다.

### 4. 한도 정하기

"내가 정한 한도"에서 두 값을 정합니다.

| 값 | 뜻 |
| --- | --- |
| 알림만 | 이 금액까지는 알림만 가고 그대로 진행됩니다 |
| 잠시 유예 | 이 금액까지는 60초 기다렸다 진행되고, 그 사이 취소할 수 있습니다 |
| (그 이상) | **내 지갑 서명 없이는 나가지 않습니다** |

처음에는 `5 / 10`으로 두고 시작해 보세요.

### 5. 일 맡기기

"여기서도 시켜볼 수 있습니다"에 필요한 것을 문장으로 적고 **맡기기**를 누릅니다.

> 예: `다음 주 팀 회의 전에 훑어볼 스테이블코인 결제 시장 요약이 필요하다`

에이전트가 카탈로그에서 일의 크기에 맞는 공급자를 고릅니다. **무엇을 왜 골랐는지, 무엇을 기각했는지**가 화면에 그대로 나옵니다.

### 6. 여기가 핵심 — 한도를 바꿔 다시 맡기기

방금 산 것이 `NOTIFY`(알림만 가고 통과)로 끝났을 겁니다. 이제 **한도를 `0 / 0`으로 낮추고 같은 요청을 다시** 넣어 보세요.

**같은 금액인데 판정이 `APPROVAL`로 바뀝니다.** 정책 엔진이 화면 문구가 아니라 실제로 작동한다는 것을 여기서 직접 확인하게 됩니다.

이제 "내 지갑으로 승인"을 누르면 서명을 요구하고, 서명해야 비로소 실행됩니다. "거부"를 누르면 돈이 나가지 않습니다.

### 7. 결과 확인

"결과물 받기(정산)"를 누르면 온체인 정산과 결과물 열람 결제(x402)까지 이어집니다. 상단 **영수증**에서 각 건의 판정·결말·트랜잭션 해시를 한 화면에 볼 수 있습니다.

---

## B. MCP까지 붙이기 (추가 5분)

**원래 설계는 일을 맡기는 것이 터미널(MCP)의 일이고, 웹은 사람이 결정하는 것과 진행 확인을 맡습니다.** 위 5번의 웹 입력창은 MCP가 없는 분을 위한 보조입니다.

### 1. 레포 받기

```bash
git clone https://github.com/waiaas/a2a-auction.git
cd a2a-auction/app && npm install
```

### 2. 토큰 복사

`#me` 화면의 "에이전트에게 시키는 정식 경로" 카드에서 **설정 복사**를 누릅니다. 내 토큰이 들어간 설정이 통째로 복사됩니다.

### 3. MCP 클라이언트에 붙이기

Claude Desktop이라면 `claude_desktop_config.json`에 붙여 넣고 재시작합니다.

```json
{
  "mcpServers": {
    "a2ahouse": {
      "command": "node",
      "args": ["<클론한 경로>/app/mcp-server.js"],
      "env": {
        "ORCHESTRATOR_URL": "https://a2a-house.8-230-9-237.nip.io",
        "A2A_TOKEN": "<복사한 토큰>"
      }
    }
  }
}
```

`args`의 경로만 본인 것으로 바꾸면 됩니다.

### 4. 도구 5종

| 도구 | 하는 일 |
| --- | --- |
| `list_skills` | 카탈로그 조회 (가격·분량·출처 수·수행 이력) |
| `request_work` | **자연어로 일 맡기기.** 에이전트가 고르고 정책이 판정합니다 |
| `purchase_skill` | 살 것을 이미 정했을 때 지정 구매 |
| `get_status` | 진행 확인 |
| `register_skill` | 능력을 카탈로그에 등록(판매자 쪽) |

이렇게 말해 보세요.

> "list_skills로 뭘 살 수 있는지 보고, request_work로 경쟁사 동향 짧은 브리핑을 맡겨줘"

**정책 판정이 도구 응답으로 그대로 돌아옵니다.** 티어 이름과 그 뜻, 다음에 벌어질 일이 함께 실립니다.

### 5. 승인은 웹에서

`APPROVAL`이 나오면 도구가 이렇게 답합니다.

> 오너의 지갑 서명이 필요하다. 도구로는 승인할 수 없다 — `.../#me` 에서 승인하거나 거부하라.

**승인은 지갑 서명이라 도구가 대행하지 않습니다.** 이 경계가 "에이전트가 할 수 있는 것과 사람이 해야 하는 것"의 선입니다.

---

## 무엇을 봐야 하는가

이 데모가 증명하려는 것은 셋입니다.

1. **에이전트가 스스로 고른다** — 무엇을 왜 골랐는지가 화면과 도구 응답에 남습니다
2. **사람이 그은 선을 넘지 못한다** — 한도를 바꾸면 같은 요청의 판정이 달라집니다
3. **넘어야 할 땐 사람에게 온다** — 승인은 지갑 서명으로만 됩니다

---

## 막히면

| 증상 | 원인과 해결 |
| --- | --- |
| 지갑 버튼이 안 보임 | 익스텐션이 늦게 주입된 경우입니다. 새로고침하거나 "임시 체험 지갑"으로 시작하세요 |
| 입금이 안 됨 | 내 지갑에 devnet SOL이 없는 경우입니다. 체험 자산을 먼저 받으세요 |
| "에이전트 지갑에 USDC가 부족합니다" | 3번(입금)을 먼저 하세요. 가장 싼 능력이 5 USDC입니다 |
| 새로고침 후 승인이 안 됨 | 지갑 연결이 끊긴 것입니다. 상단 배너에서 다시 연결하면 됩니다(서버 세션은 유지됩니다) |
| MCP에서 401 | 토큰이 만료됐습니다. `#me`에서 다시 연결하고 설정을 새로 복사하세요 |
| 판단이 "규칙으로 선택"으로 뜸 | 모델 호출이 실패해 규칙 기반으로 내려간 것입니다. 흐름은 그대로 진행됩니다 |

---

## 피드백

써보시고 이상하거나 막히는 지점을 알려주세요. 특히 이 셋이 궁금합니다.

- 어느 단계에서 무엇을 해야 할지 몰라 멈췄는지
- 판정이 왜 그렇게 났는지 화면만 보고 이해되는지
- 발표에서 보여줄 순서로 이게 맞는지
