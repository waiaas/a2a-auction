# 데모 백엔드 (app/)

이 폴더의 실행 방법·API·환경 변수는 **[루트 README](../README.md)의 "실행 방법" 절**이 정본입니다. 여기에 따로 적지 않습니다(문서가 두 곳이면 한쪽만 갱신되는 사고가 납니다. 실제로 이 파일이 그랬습니다).

빠른 단발 실행 하나만 여기 남깁니다. 서버 없이 고정 라운드의 코어 플로우만 한 번 돌려 3분기 판정을 확인합니다:

```bash
node demo-path.js   # create → commit ×3 → deposit ×3 → reveal A → settle
```
